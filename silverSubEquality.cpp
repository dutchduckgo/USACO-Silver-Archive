#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define vl vector<ll>

string s, t;
ll q;
vector<pair<char, ll>> map;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    cout.tie(0);
    cin >> s >> t >> q; 
    map.resize(q);
    
    for (ll i = 0; i < q; i ++) {
        char temp;
        cin >> temp;

    }   
    

}