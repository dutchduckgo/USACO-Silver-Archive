#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define vl vector<ll>

int main() {
    //freopen("filename.in", "r", stdin);
    //freopen("filename.out", "w", stdout);
    ll n, m;
    cin >> n >> m;
    vector < pair<ll, ll>> cows;
    cows.resize(n);
    for (int i = 0; i < n; i ++) {
        cin >> cows[i].first;
        cin >> cows[i].second;
    }
    
}